package com.newnormallist.newsservice.recommendation.controller;

// RecommendationService를 호출해 최종 피드(뉴스 리스트) DTO로 반환
// 요청 시 계산을 최소화하고, 이미 DB에 저장된 사용자 벡터를 사용해 빠르게 조립
public class FeedController {
    
}
